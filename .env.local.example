# Copy this file to `.env.local` and fill in the real values.
# `.env.local` is gitignored — the keys below must never be committed or shared.
#
#   cp .env.local.example .env.local
#
# Restart `npm run dev` after changing anything here.

# --- Admin area (required) ---------------------------------------------------
# Guards /admin and the photo APIs behind it, including the one that serves
# unwatermarked originals. Without this set, the admin area stays locked.
#
# Your browser asks for it with its own login box — leave the username blank and
# type this as the password.
#
# Pick something long and random, not a word you also use elsewhere:
#     openssl rand -base64 24
#
# Server-only. It must NEVER be prefixed with NEXT_PUBLIC_ — that would send it
# to every visitor's browser.
ADMIN_PASSWORD=replace_me_with_a_long_random_password

# --- Where live data lives (server: required) ---------------------------------
# The catalogue and the watermarked renders. Leave these unset locally and they
# default to src/data/photos.json and public/photos/, which is fine for a
# checkout on your own machine.
#
# On the server they MUST point outside /srv/hkp/site, because that is the
# directory the deploy runs `git pull` in. With the data inside it, `git clean
# -fd` deleted all 254 renders, and `git reset --hard` would have reset the
# catalogue to empty — both have been real incidents.
#
# CATALOGUE_FILE=/srv/hkp/data/photos.json
# RENDERS_DIR=/srv/hkp/renders

# --- Photo storage (required) ------------------------------------------------
# Full-resolution originals live in a Backblaze B2 bucket, not on this server.
# B2 speaks the S3 protocol, so Cloudflare R2 or Amazon S3 work here too — only
# these five values change.
#
# The bucket must be PRIVATE. A public bucket hands out unwatermarked originals
# to anyone who can guess a URL, which is the thing customers are paying for.
#
# Bucket name, exactly as it appears in the B2 dashboard.
B2_BUCKET=
#
# From the bucket's details in the dashboard, with https:// in front.
# Looks like https://s3.eu-central-003.backblazeb2.com
B2_ENDPOINT=
#
# The region embedded in that endpoint — e.g. eu-central-003
B2_REGION=
#
# B2 dashboard → Application Keys → Add a New Application Key. Restrict it to
# the one bucket. The secret is shown ONCE, at creation — save it then.
# Server-only. Neither may ever be prefixed with NEXT_PUBLIC_.
B2_KEY_ID=
B2_APP_KEY=

# --- Stripe (required) -------------------------------------------------------
# Dashboard → Developers → API keys → "Secret key".
# Starts with sk_test_ while you're testing, sk_live_ once you go live.
# This is server-only. It must NEVER be prefixed with NEXT_PUBLIC_.
STRIPE_SECRET_KEY=sk_test_replace_me

# Verifies that incoming webhooks really came from Stripe.
# Local dev: run `stripe listen --forward-to localhost:3000/api/stripe/webhook`
#            and copy the whsec_ value it prints.
# Production: Dashboard → Developers → Webhooks → your endpoint → signing secret.
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Where Stripe sends the customer back to. No trailing slash.
# Change to https://yourdomain.com when you deploy.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# --- Email (optional) --------------------------------------------------------
# Without these, purchases still work and download links still appear on the
# success page — the confirmation email is simply skipped.
# Sign up at https://resend.com, then API Keys → Create.
RESEND_API_KEY=

# The "from" address. It must sit on a domain verified with Resend — a gmail.com
# address will not work, because sending as gmail.com requires DNS records only
# Google can set. The address needs no mailbox of its own.
EMAIL_FROM=h_kivimurd Photography <photos@helenkivimurd.com>

# Where replies go. The receipt says "just reply to this email", and the From
# address above has no inbox behind it, so without this those replies vanish.
EMAIL_REPLY_TO=helen.kivimurd@gmail.com
